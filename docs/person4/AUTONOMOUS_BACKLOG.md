# Person 4 autonomous backlog

This list separates work Person 4 can complete alone from work that requires a handoff.

## Completed on the isolated branch

- versioned P2/P3/confidence/P5/P6 request contract;
- deterministic MCDA and component contributions;
- confidence-calibrated rank acceptability (`ccra-v1`);
- stable, review-required, unstable and excluded states;
- deterministic explanations and numeric traces;
- synthetic baseline evaluation metrics;
- frozen feature dictionary and named weight presets;
- area, geometry, shading, distance, budget and required-cost filters;
- Pareto-front membership;
- input-version alignment and output version trace;
- synchronized JSON Schemas;
- fixture runner, narrow tests and a 5,000-candidate benchmark command.
- machine-readable exclusion reason catalog and output consistency validation;
- ML challenger promote/no-promote report template.

## Still autonomous

1. Add generated boundary tests for complex Polygon/MultiPolygon geometry.
2. Profile memory and runtime using 5,000 candidates at several iteration counts.
3. Optimize the rank simulation only if the stored benchmark identifies a demo risk.

## Requires another owner

- real P2/P3 batch integration and any change to their component construction;
- GOBS-present versus GOBS-masked ablation, until source availability is supplied;
- API/PostGIS/GeoJSON wiring by Person 5;
- real Precision@K/Recall@K/nDCG claims using locked Person 6 labels;
- ML challenger training, which requires adequate labels and a held-out split;
- field, structural, legal or utility validation.
